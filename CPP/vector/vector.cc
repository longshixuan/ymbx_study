// #include "vector.hpp"``
#include<iostream>
#include<vector>
using namespace std;

int main()
{
    
    // ymbx::Test();
    // int a = 1;
    // int b = 2;
    // cout << a <<" "<< b << endl;
    // cout << &a << " " << &b << endl;
    // swap(a, b);
    // cout << a << " " << b << endl;
    // cout << &a << " " << &b << endl;
    // vector<int> count(10);
    // cout << "size:" << count.size() << endl;
    // cout << "capacity:" << count.capacity() << endl;

    // count.clear();
    // cout << "clear_size:" << count.size() << endl;
    // cout << "clear_capacity:" << count.capacity() << endl;

    return 0;
}
